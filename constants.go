package main

// Just some useful constants
const MaxUint = ^uint(0)
const MinUint = 0
const MaxInt = int(MaxUint >> 1)
const MinInt = -MaxInt - 1

// URL prefix of map website
const UrlPrefix string = "http://www.gcmap.com/mapui?P="
